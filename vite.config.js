import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  // This ensures your assets (JS, CSS) load correctly from the /gpu-paint/ subfolder
  base: '/gpu-paint/', 
})